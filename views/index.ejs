<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Animal Images</title>
    <link type="image/png" rel="icon" href="assets/favicon.png">
    <link rel="stylesheet" type="text/css" href="styles/main.css">
</head>
<body>
    <form action="/images" method="GET">
        <label for="type">Choose animal type:</label>
        <select name="type" id="type" required>
            <option value="shibes">Shibes</option>
            <option value="cats">Cats</option>
            <option value="birds">Birds</option>
        </select>

        <label for="count">Count (1-100):</label>
        <input type="number" id="count" name="count" min="1" max="100" value="1" required>

        <input type="submit" value="Fetch Images">
    </form>

    <div>
        <% if (locals.url) { %>
            <% url.forEach(imageUrl => { %>
                <img src="<%= imageUrl %>" alt="Animal Image">
            <% }); %>
        <% } %>
    </div>
</body>
</html>
